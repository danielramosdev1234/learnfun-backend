# Force Node.js detection (not Python)
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "npm ci",
    "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "npm run start"