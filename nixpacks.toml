# nixpacks.toml - Node.js + Python hybrid configuration

[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "npm ci",
    "pip install --user -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "node src/server.js"